apiVersion: v1
kind: Service
metadata:
  name: support-restaurants-backend
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
  selector:
    app: support-restaurants-backend
